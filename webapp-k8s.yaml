apiVersion: apps/v1
kind: Deployment
metadata:
  name: node-app-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: node-app
  template:
    metadata:
      labels:
        app: node-app
    spec:
      containers:
      - name: web-app
        image: nodejs-demo-app:local
        imagePullPolicy: Never
        ports:
        - containerPort: 3000
        env:
        # Указываем DNS-имя базы данных, которое мы создали в mongo-k8s.yaml
        - name: TODO_MONGO_CONNSTR
          value: "mongodb://mongodb-service:27017/demoapp"
---
apiVersion: v1
kind: Service
metadata:
  name: node-app-service
spec:
  type: LoadBalancer
  selector:
    app: node-app
  ports:
    - name: web
      protocol: TCP
      port: 3005
      targetPort: 3000
